#!/bin/bash
# Скрипт для добавления SSH ключа на сервер

SERVER_USER="administrator"
SERVER_HOST="85.190.102.101"
PUBLIC_KEY_FILE="$HOME/.ssh/rainbow_say_deploy.pub"

if [ ! -f "$PUBLIC_KEY_FILE" ]; then
    echo "❌ Публичный ключ не найден: $PUBLIC_KEY_FILE"
    exit 1
fi

echo "🔑 Публичный ключ для добавления на сервер:"
echo ""
cat "$PUBLIC_KEY_FILE"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📋 Выполни эту команду (потребуется ввести пароль один раз):"
echo ""
echo "ssh $SERVER_USER@$SERVER_HOST 'mkdir -p ~/.ssh && echo \"$(cat $PUBLIC_KEY_FILE)\" >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys && chmod 700 ~/.ssh && echo \"✅ Ключ добавлен!\"'"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Или выполни вручную на сервере:"
echo ""
echo "1. Подключись: ssh $SERVER_USER@$SERVER_HOST"
echo "2. Выполни:"
echo "   mkdir -p ~/.ssh"
echo "   echo '$(cat $PUBLIC_KEY_FILE)' >> ~/.ssh/authorized_keys"
echo "   chmod 600 ~/.ssh/authorized_keys"
echo "   chmod 700 ~/.ssh"
echo ""
echo "После этого деплой будет работать автоматически!"

