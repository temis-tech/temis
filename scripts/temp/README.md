# 📁 Временные скрипты

Эта папка для временных и отладочных скриптов, которые **НЕ попадают в git**.

## Что сюда класть:

- ✅ Временные скрипты для отладки
- ✅ Личные скрипты для настройки окружения
- ✅ Скрипты для тестирования
- ✅ Вспомогательные скрипты, которые не нужны на сервере

## Что НЕ сюда:

- ❌ Скрипты деплоя (в `scripts/`)
- ❌ Скрипты настройки сервера (в `scripts/`)
- ❌ Скрипты для работы с данными (в `scripts/`)

## Примеры:

```bash
# Создать временный скрипт
cat > scripts/temp/test-connection.sh << 'EOF'
#!/bin/bash
ssh root@2a03:6f01:1:2::1:f3f5 "echo 'Test'"
EOF
chmod +x scripts/temp/test-connection.sh
```

Все файлы в этой папке автоматически игнорируются git (см. `.gitignore`).

