# 🚀 Быстрый доступ к сайту

## По IP адресу (прямой доступ)

После того как файлы загружены и сервисы запущены, можно проверить сайт напрямую:

### Фронтенд (Next.js)
```
http://85.190.102.101:3001
```

### API (Django)
```
http://85.190.102.101:8001/api/
```

### Проверка на сервере

Подключись к серверу и проверь:

```bash
ssh administrator@85.190.102.101

# Проверь, что сервисы запущены
sudo systemctl status rainbow-say-frontend
sudo systemctl status rainbow-say-backend

# Проверь, что порты открыты
sudo netstat -tlnp | grep -E ':(3001|8001)'

# Проверь локально
curl http://localhost:3001
curl http://localhost:8001/api/
```

## ⚠️ Важно

1. **Порты могут быть закрыты файрволом** - проверь настройки файрвола
2. **Сервисы должны быть запущены** - используй systemd сервисы
3. **Для продакшена лучше использовать домен** - настрой Nginx

## Настройка файрвола (если порты закрыты)

```bash
# На сервере
sudo ufw allow 3001/tcp
sudo ufw allow 8001/tcp
sudo ufw reload
```

## После настройки Nginx

Когда Nginx настроен, сайт будет доступен по домену:
- `https://rainbow-say.estenomada.es` (фронтенд)
- `https://api.rainbow-say.estenomada.es/api/` (API)

И порты 3001 и 8001 можно закрыть в файрволе, так как Nginx будет проксировать запросы.

