# 🚀 Быстрое исправление проблем на сервере

## Вариант 1: Через GitHub Actions (рекомендуется)

1. Перейди в GitHub репозиторий: https://github.com/temis-tech/temis
2. Перейди в **Actions** → **Fix Server Issues**
3. Нажми **Run workflow** → **Run workflow**
4. Дождись завершения (обычно 1-2 минуты)

## Вариант 2: Напрямую на сервере

Подключись к серверу и выполни:

```bash
ssh administrator@<SERVER_IP>
sudo bash /var/www/temis/deploy/fix-server.sh
```

Если скрипт еще не загружен, сначала выполни деплой через GitHub Actions.

## Что делает скрипт:

1. ✅ Исправляет права доступа к файлам
2. ✅ Создает .env файл если его нет
3. ✅ Создает systemd сервисы (temis-frontend, temis-backend)
4. ✅ Применяет миграции Django
5. ✅ Собирает статику Django
6. ✅ Настраивает Nginx конфигурацию
7. ✅ Перезапускает все сервисы
8. ✅ Проверяет статус и порты

## После исправления:

- Сайт должен быть доступен: http://temis.ooo
- Админка: http://api.temis.ooo/admin/
- Статические файлы должны загружаться

## Если проблемы остались:

Проверь логи:
```bash
sudo journalctl -u temis-frontend -n 50
sudo journalctl -u temis-backend -n 50
sudo tail -f /var/log/nginx/temis_error.log
```

