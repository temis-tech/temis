# 🧪 Тестирование интеграции с MoyKlass

## Быстрая проверка

### 1. Проверка настроек в админке

1. Откройте `/admin/moyklass/moyklasssettings/`
2. Убедитесь, что:
   - ✅ Интеграция активна (`is_active = True`)
   - ✅ API ключ введен
   - ✅ Токен действителен (зеленый статус)

### 2. Тестирование подключения

Используйте команду управления:

```bash
# На сервере
cd /var/www/temis/backend
source venv/bin/activate
python manage.py test_moyklass
```

Эта команда проверит:
- ✅ Подключение к API MoyKlass
- ✅ Получение информации о компании
- ✅ Статус токена доступа

### 3. Создание тестового лида

```bash
# Создать тестового ученика в MoyKlass
python manage.py test_moyklass --create-student
```

### 4. Просмотр списка лидов

```bash
# Показать список учеников из MoyKlass
python manage.py test_moyklass --list-students
```

## Проверка автоматического создания лидов

### Способ 1: Через форму записи на сайте

1. Откройте сайт: `https://temis.estenomada.es`
2. Заполните форму записи (например, на главной странице или в каталоге)
3. Отправьте форму
4. Проверьте в MoyKlass CRM, что лид создан

### Способ 2: Через админку Django

1. Откройте `/admin/booking/bookingsubmission/`
2. Создайте новую отправку формы вручную
3. Проверьте логи в `/admin/moyklass/moyklassrequestlog/`
4. Проверьте в MoyKlass CRM, что лид создан

### Способ 3: Через API напрямую

```bash
# Отправить форму через API
curl -X POST https://api.temis.estenomada.es/api/booking/submissions/ \
  -H "Content-Type: application/json" \
  -d '{
    "form_id": 1,
    "service_id": 1,
    "data": {
      "name": "Тестовый Клиент",
      "phone": "+79001234567",
      "email": "test@example.com",
      "description": "Тестовая запись"
    }
  }'
```

## Проверка логов

### В админке Django

1. `/admin/moyklass/moyklassrequestlog/` - все запросы к API MoyKlass
2. `/admin/moyklass/moyklasssynclog/` - логи синхронизации
3. `/admin/booking/bookingsubmission/` - все отправки форм

### На сервере

```bash
# Логи Django (если настроено логирование)
tail -f /var/log/temis-backend-error.log

# Логи systemd
sudo journalctl -u temis-backend -f
```

## Что проверить после создания лида

1. ✅ Лид появился в MoyKlass CRM
2. ✅ Данные корректны (имя, телефон, email)
3. ✅ Комментарий содержит информацию об услуге
4. ✅ В логах запросов (`/admin/moyklass/moyklassrequestlog/`) есть запись о создании
5. ✅ Статус ответа 200 или 201

## Возможные проблемы

### Лид не создается

1. **Проверьте настройки:**
   - Интеграция активна?
   - API ключ правильный?
   - Токен действителен?

2. **Проверьте логи:**
   - `/admin/moyklass/moyklassrequestlog/` - есть ли ошибки?
   - Логи сервера - есть ли исключения?

3. **Проверьте данные формы:**
   - Поле `name` заполнено?
   - Поле `phone` заполнено?
   - Формат данных корректный?

### Ошибка авторизации

1. Обновите токен в админке (кнопка "Обновить токен доступа")
2. Проверьте API ключ в настройках MoyKlass CRM
3. Проверьте, что API ключ не истек

### Лид создается, но данные неполные

1. Проверьте названия полей в форме записи
2. Убедитесь, что поля `name`, `phone`, `email` имеют правильные `name` атрибуты
3. Проверьте маппинг полей в `backend/booking/signals.py`

## Отладка

Включите подробное логирование в `backend/booking/signals.py`:

```python
import logging
logger = logging.getLogger(__name__)
logger.setLevel(logging.DEBUG)
```

Затем проверьте логи при создании формы записи.

